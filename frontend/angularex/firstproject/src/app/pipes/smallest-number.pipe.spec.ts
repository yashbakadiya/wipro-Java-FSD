import { SmallestNumberPipe } from './smallest-number.pipe';

describe('SmallestNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new SmallestNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
